# AI Career Guidance Platform

Platform bimbingan karir berbasis AI untuk siswa kelas 12.

## Cara Menjalankan
1. Install dependencies: `pip install -r requirements.txt`
2. Run aplikasi: `streamlit run app.py`